import { ImperiaTableColumnDefaultConfigurationDirective } from './imperia-table-column-default-configuration.directive';

describe('ImperiaTableColumnDefaultConfigurationDirective', () => {
  it('should create an instance', () => {
    const directive = new ImperiaTableColumnDefaultConfigurationDirective();
    expect(directive).toBeTruthy();
  });
});
