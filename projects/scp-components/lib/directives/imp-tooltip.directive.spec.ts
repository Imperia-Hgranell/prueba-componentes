import { ImpTooltipDirective } from './imp-tooltip.directive';

describe('ImpTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
