import { FindOptionPipe } from './find-option.pipe';

describe('FindOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FindOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
