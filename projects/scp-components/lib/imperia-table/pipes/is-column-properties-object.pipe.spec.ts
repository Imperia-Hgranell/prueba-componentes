import { IsColumnPropertiesObjectPipe } from './is-column-properties-object.pipe';

describe('IsColumnPropertiesObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new IsColumnPropertiesObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
