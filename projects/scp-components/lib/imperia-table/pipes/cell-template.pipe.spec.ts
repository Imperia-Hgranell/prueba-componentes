import { CellTemplatePipe } from './cell-template.pipe';

describe('CellContentPipe', () => {
  it('create an instance', () => {
    const pipe = new CellTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
