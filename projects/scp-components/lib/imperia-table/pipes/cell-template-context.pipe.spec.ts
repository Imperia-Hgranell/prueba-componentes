import { CellTemplateContextPipe } from './cell-template-context.pipe';

describe('CellContextPipe', () => {
  it('create an instance', () => {
    const pipe = new CellTemplateContextPipe();
    expect(pipe).toBeTruthy();
  });
});
