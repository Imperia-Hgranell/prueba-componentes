import { BodyCellContextMenuDirective } from './body-cell-context-menu.directive';

describe('BodyCellContextMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyCellContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
