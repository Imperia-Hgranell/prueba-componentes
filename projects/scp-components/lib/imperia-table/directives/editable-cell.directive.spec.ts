import { EditableCellDirective } from './editable-cell.directive';

describe('EditableCellDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableCellDirective();
    expect(directive).toBeTruthy();
  });
});
