import { ResizableColumnV2Directive } from './resizable-column-v2-directive.directive';

describe('ResizableColumnV2DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableColumnV2Directive();
    expect(directive).toBeTruthy();
  });
});
