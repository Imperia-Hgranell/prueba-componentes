import { HeaderCellContextMenuDirective } from './header-cell-context-menu.directive';

describe('HeaderCellContextMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderCellContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
