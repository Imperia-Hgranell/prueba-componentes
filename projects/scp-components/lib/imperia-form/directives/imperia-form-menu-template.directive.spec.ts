import { ImperiaFormMenuTemplateDirective } from './imperia-form-menu-template.directive';

describe('ImperiaFormMenuTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ImperiaFormMenuTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
