import { ImperiaFormTemplateDirective } from './imperia-form-template.directive';

describe('ImperiaFormTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ImperiaFormTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
