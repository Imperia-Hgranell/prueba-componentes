import { TimeTypeToStringPipe } from './time-type-to-string.pipe';

describe('TimeTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
