import { ImpMenuItemDirective } from './imp-menu-item.directive';

describe('ImpMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpMenuItemDirective();
    expect(directive).toBeTruthy();
  });
});
