import { ImpDisabledDirective } from './imp-disabled.directive';

describe('ImpDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
