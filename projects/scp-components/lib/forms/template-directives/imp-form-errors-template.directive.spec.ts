import { ImpFormErrorsTemplateDirective } from './imp-form-errors-template.directive';

describe('ImpFormErrorsTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpFormErrorsTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
