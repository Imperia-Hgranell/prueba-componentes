import { IsFilterSelectedPipe } from './is-filter-selected.pipe';

describe('IsFilterSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFilterSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
