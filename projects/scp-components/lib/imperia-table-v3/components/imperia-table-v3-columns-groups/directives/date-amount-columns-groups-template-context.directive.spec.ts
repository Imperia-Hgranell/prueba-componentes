import { DateAmountColumnsGroupsTemplateContextDirective } from './date-amount-columns-groups-template-context.directive';

describe('DateAmountColumnsGroupsTemplateContextDirective', () => {
  it('should create an instance', () => {
    const directive = new DateAmountColumnsGroupsTemplateContextDirective();
    expect(directive).toBeTruthy();
  });
});
