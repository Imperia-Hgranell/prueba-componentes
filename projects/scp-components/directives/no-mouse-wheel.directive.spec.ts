import { NoMouseWheelDirective } from './no-mouse-wheel.directive';

describe('NoMouseWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new NoMouseWheelDirective();
    expect(directive).toBeTruthy();
  });
});
