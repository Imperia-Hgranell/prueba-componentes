import { IsContentOverflowingDirective } from './is-content-overflowing.directive';

describe('IsContentOverflowingDirective', () => {
  it('should create an instance', () => {
    const directive = new IsContentOverflowingDirective();
    expect(directive).toBeTruthy();
  });
});
