import { ImpCardResizeDirective } from './imp-card-resize.directive';

describe('ImpCardResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpCardResizeDirective();
    expect(directive).toBeTruthy();
  });
});
