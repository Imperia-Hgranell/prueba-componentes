import { TooltipOnHoverDirective } from './tooltip-on-hover.directive';

describe('TooltipOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
