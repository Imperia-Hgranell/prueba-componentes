import { CdkMenuItemKeepOpenDirective } from './cdk-menu-item-keep-open.directive';
import { CdkMenuItem } from '@angular/cdk/menu';

describe('CdkMenuItemKeepOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkMenuItemKeepOpenDirective();
    expect(directive).toBeTruthy();
  });
});
