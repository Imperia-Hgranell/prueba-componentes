import { CdkMenuTriggerSafeCloseDirective } from './cdk-menu-trigger-safe-close.directive';

describe('CdkMenuTriggerSafeCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkMenuTriggerSafeCloseDirective();
    expect(directive).toBeTruthy();
  });
});
