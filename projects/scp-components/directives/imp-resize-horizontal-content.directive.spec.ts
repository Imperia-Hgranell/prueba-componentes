import { ImpResizeVerticalContentDirective } from '@shared/directives/imp-resize-vertical-content.directive';

describe('ImpResizeVerticalContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpResizeVerticalContentDirective();
    expect(directive).toBeTruthy();
  });
});
