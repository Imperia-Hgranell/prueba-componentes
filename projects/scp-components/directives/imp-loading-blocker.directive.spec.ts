import { ImpLoadingBlockerDirective } from './imp-loading-blocker.directive';

describe('ImpLoadingBlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpLoadingBlockerDirective();
    expect(directive).toBeTruthy();
  });
});
