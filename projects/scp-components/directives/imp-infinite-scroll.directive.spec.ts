import { ImpInfiniteScrollDirective } from './imp-infinite-scroll.directive';

describe('ImpInfiniteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpInfiniteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
