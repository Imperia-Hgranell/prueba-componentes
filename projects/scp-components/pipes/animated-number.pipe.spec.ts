import { AnimatedNumberPipe } from './animated-number.pipe';

describe('AnimatedNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimatedNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
