import { FormatNumberToPipe } from './format-number-to.pipe';

describe('FormatNumberToPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberToPipe();
    expect(pipe).toBeTruthy();
  });
});
