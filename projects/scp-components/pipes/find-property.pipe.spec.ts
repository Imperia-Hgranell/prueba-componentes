import { FindPropertyPipe } from './find-property.pipe';

describe('FindPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new FindPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
