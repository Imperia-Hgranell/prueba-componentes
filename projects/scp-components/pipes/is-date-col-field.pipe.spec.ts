import { IsDateColFieldPipe } from './is-date-col-field.pipe';

describe('IsDateColFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new IsDateColFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
