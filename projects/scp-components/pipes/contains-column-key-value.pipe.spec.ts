import { ContainsColumnKeyValuePipe } from './contains-column-key-value.pipe';

describe('ContainsColumnKeyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainsColumnKeyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
